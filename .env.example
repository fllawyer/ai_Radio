# Claudio 云端配置示例
PORT=3000
NODE_ENV=production

# 推荐云端用环境变量配置，不要写进 state.json 或 Git
DEEPSEEK_API_KEY=sk-xxxx

# 可选：独立部署 NeteaseCloudMusicApi 时填写，例如 http://127.0.0.1:3001
NCM_API_BASE=

# 可选：持久化状态文件路径
# Docker Compose 会覆盖为 /app/data/state.json；PM2/本地可用默认 state.json
STATE_FILE=state.json
